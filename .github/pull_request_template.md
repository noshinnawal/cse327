## What changed

Brief description of the change and why.

## How it was tested

- [ ] `composer test` passes locally (or `C:\xampp\php\php.exe tests\run.php`)
- [ ] `php -l` clean on changed files
- [ ] `composer analyse` (PHPStan) clean
- [ ] `composer fix --dry-run` (PHP CS Fixer) clean
- [ ] Manually smoke-tested (if UI or handler flow changed)

## Requirement traceability

Which requirement(s) does this change? (`docs/requirements.md`, e.g. FR-04 / NFR-01)

## Notes for the reviewer

Anything specific to look at, or screenshots.
